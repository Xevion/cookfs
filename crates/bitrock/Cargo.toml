[package]
name = "bitrock"
description = "Reader for BitRock and VMware InstallBuilder installers, recovering the full payload tree with permissions intact"
keywords = ["bitrock", "installbuilder", "installer", "unpack", "extract"]
categories = ["filesystem", "parser-implementations"]
readme = "README.md"
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]

[dev-dependencies]
assert2.workspace = true
